# Aboard 1.0.2

这次更新修复左侧 Recents 偶尔只显示少量会话、稍后才补齐的问题。

## 修复内容

- Aboard 打开时会在本地预热一次 Recents 虚拟列表，让已缓存的会话立即完整显示。
- 不会新增会话接口请求，不会加重 ChatGPT/Codex 的网络负载。
- 预热后会恢复原来的侧栏滚动位置，不影响用户正在看的区域。
- 当官方侧栏重建或本地会话缓存增长时，只补做一次必要的预热。

## 安装

1. 下载 `Aboard-macOS-1.0.2.zip`。
2. 解压后打开 **Aboard** 文件夹。
3. 双击 **Install Aboard.command**。

如果 macOS 阻止打开，请按住 Control 点击安装器，选择“打开”。

安装前需要官方 ChatGPT/Codex macOS 应用和 Python 3。

## 隐私

发布包只有空白看板和合成演示数据，不包含用户项目、真实标题、会话 ID、日志、缓存、数据库或本机路径。
