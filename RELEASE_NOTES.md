# Aboard 1.0.3

这次更新让 Aboard 回到最简单的定位：只负责整理和查找，点击后直接去官方客户端继续会话。

## 使用体验

- 点击任意 Chat 或 Work，都会直接在官方 ChatGPT/Codex 客户端打开对应会话。
- 进行中、等待确认或等待输入的会话也能直接打开，不再因为运行状态被拦住。
- 不再使用 Aboard 内部会话页，也不再显示额外的悬浮返回按钮。
- 需要回到看板时，点击 Plugins 下的 Aboard；同一个运行中会话之后仍可再次打开。
- GitHub 说明页新增了拖拽分类、项目移动、进度、隐藏名称和优先级截图。

## 安装

1. 下载 `Aboard-macOS-1.0.3.zip`。
2. 解压后打开 **Aboard** 文件夹。
3. 双击 **Install Aboard.command**。

如果 macOS 阻止打开，请按住 Control 点击安装器，选择“打开”。

安装前需要官方 ChatGPT/Codex macOS 应用和 Python 3。

## 隐私

发布包只有空白看板和合成演示数据，不包含用户项目、真实标题、会话 ID、日志、缓存、数据库或本机路径。
