# Aboard 1.0.4

这次更新恢复了 Aboard 最重要的体验：看板和会话都在同一个 Aboard 窗口里完成，不再在两个 App 之间来回跳。

## 使用体验

- 点击 Chat 或 Work，会在当前 Aboard 窗口打开原会话。
- 点击 Plugins 下的 Aboard 即可回到看板；看板只覆盖会话，不会把正在思考的会话卸载。
- 回到看板后马上再点同一条，会直接恢复原页面，不会重复加载或重新抢占任务。
- 从看板新建的 Chat 或 Work 会留在原生会话页继续对话，并自动归入刚才选择的分类或项目。
- “进行中、等待批准、等待输入”和上下文进度现在会正确合并，不会互相覆盖。
- 本地 Work 若确实由另一个 Codex 进程写入，Aboard 会留在看板并明确提示，不会自动跳 App 或冒险双写。

## 安装

1. 下载 `Aboard-macOS-1.0.4.zip`。
2. 解压后打开 **Aboard** 文件夹。
3. 双击 **Install Aboard.command**。

如果 macOS 阻止打开，请按住 Control 点击安装器，选择“打开”。

安装前需要官方 ChatGPT/Codex macOS 应用和 Python 3。

## 隐私

发布包只有空白看板和合成演示数据，不包含用户项目、真实标题、会话 ID、日志、缓存、数据库或本机路径。
